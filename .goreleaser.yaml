# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: kafka
    binary: kafka
    buildmode: "c-shared"
    flags:
      - -tags=zig
    ldflags:
    - -s -w -X github.com/litesql/kafka/extension.version={{.Version}} -X github.com/litesql/kafka/extension.commit={{.Commit}} -X github.com/litesql/kafka/extension.date={{.Date}}
    goos:
      - linux
    goarch:
      - amd64
    env:
      - CGO_ENABLED=1
      - CC=zig cc -target x86_64-linux-gnu

  - id: kafka_linux_arm64
    binary: kafka
    buildmode: "c-shared"
    flags:
      - -tags=zig
    ldflags:
    - -s -w -X github.com/litesql/kafka/extension.version={{.Version}} -X github.com/litesql/kafka/extension.commit={{.Commit}} -X github.com/litesql/kafka/extension.date={{.Date}}
    goos:
      - linux
    goarch:
      - amd64
    env:
      - CGO_ENABLED=1
      - CC=zig cc -target aarch64-linux-gnu

  - id: kafka_windows
    binary: kafka
    buildmode: "c-shared"
    flags:
      - -tags=zig
    ldflags:
    - -s -w -X github.com/litesql/kafka/extension.version={{.Version}} -X github.com/litesql/kafka/extension.commit={{.Commit}} -X github.com/litesql/kafka/extension.date={{.Date}}
    goos:
      - windows
    goarch:
      - amd64
    env:
      - CGO_ENABLED=1
      - CC=zig cc -target x86_64-windows

  - id: kafka_darwin
    binary: kafka
    buildmode: "c-shared"
    ldflags:
    - -s -w -X github.com/litesql/kafka/extension.version={{.Version}} -X github.com/litesql/kafka/extension.commit={{.Commit}} -X github.com/litesql/kafka/extension.date={{.Date}}
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=1
      - CC=o64-clang

archives:
  - id: kafka
    formats: [tar.gz]
    ids: [kafka, kafka_linux_arm64, kafka_windows, kafka_darwin]
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      kafka_v
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - none*

changelog:
  disable: true

release:  
  github:
    owner: litesql
    name: kafka

  draft: false